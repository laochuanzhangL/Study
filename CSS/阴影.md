
# 文字阴影
```css
text-shadow:1px 1px 3px red
第一个参数是阴影的水平方向位移量，大于0时向右
第二个参数是阴影的垂直方向位移量，大于0时向下
第三个参数是模糊度
第四个参数是颜色
可以有多个阴影，之间用,连接
```

# 盒子阴影
```css
box-shadow:1px 1px 3px red//此时和文字阴影的参数意思一样

box-shadow:0px 0px 10px red//盒子四周会有相同的阴影

box-shadow:10px 10px 5px inset red//内置阴影
```
